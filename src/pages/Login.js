const Login = () => {
    return (
        <div>
            <div>
                <form>
                    <input placeholder="아이디" />
                    <input placeholder="비밀번호" />
                </form>
                <button>로그인</button>
                <div>
                    <button>계정 생성</button>
                </div>
            </div>
        </div>
    );
};

export default Login;
